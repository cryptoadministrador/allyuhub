*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/*.patch
/coverage
/deploy/.env.db
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# PDFs oficiales del currículo: pesados y con copyright — se descargan localmente
storage/curriculo/*.pdf

# Documentos de trabajo que NO son el repo. El plan maestro y la arquitectura
# viven en el proyecto Claude «e-skool» (ver CLAUDE.md); las misiones de
# Antigravity son de un solo uso y quedan obsoletas en cuanto se mergea el PR.
# Si se versionan, el repo empieza a tener dos fuentes de verdad divergentes.
/goal-anty-*.md
/allyuhub-plan-maestro.md
/e-skool-arquitectura.md

# Informe HTML de cobertura de Vitest: artefacto de build, se regenera con
# `npm run test:js -- --coverage`. Son ~25 ficheros que ensucian cada diff.

# Prototipo interactivo (demo de Cowork): archivo de un solo uso, no del repo.
/allyuhub-prototipo.html
